<!DOCTYPE html>
<html>
    <head>
        <meta charset = "utf-8">
        <!--JQuery-->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
        <!-- Latest compiled and minified CSS -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <!-- Latest compiled and minified JavaScript -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
        
        <link rel='stylesheet' href='css/insertWord.css' />
        <title>insertWord</title>
    </head>
    <body>
        <nav class = "navbar navbar-default">
            <div class = "container-fluid">
                <div class="navbar-header">
                    <form id="search" role="search" action="/search" method="post">
                        <div class="input-group">
                            <input type="text" class="form-control" name="searchWord" placeholder="Search..." required="true">
                            <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
                        </div>
                    </form>
                    <% if (loginStatus){ %>
                        <a class = "navbar-brand" href="./insertWord"><%= user.nickname %>  Home</a>
                        <a href="/signout" class="btn btn-default navbar-btn pull-right">登出</a>
                    <% }else{ %>
                        <a class = "navbar-brand">Home</a>
                        <a href="/" class="btn btn-default navbar-btn pull-right">登入</a>
                        <a href="/getsignup" class="btn btn-default navbar-btn pull-right">註冊</a>
                    <% } %>
                </div>
            </div>
        </nav>
        <section id="vocaulary list">
            <div class="container">
                    <a href="/insertLesson" type="button" class="btn btn-primary" ><span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span> 回到課程</a>
                <% if (loginStatus){ %>
                    <h3>新增單字</h3>
                    <form class="form-horizontal" role="form" action="/lesson/<%= lessonindex %>/addword" method="post">
                        <div class="input-group">
                            <input type="text" class="form-control" name="word" id="word" placeholder="請輸入單字" required="true" autofocus>                
                            <span class="input-group-btn">
                                <button type = "submit" class="btn btn-default ">儲存變更</button>
                            </span>
                        </div>
                    </form>
                <% } %>
                <h3>單字列表</h3>
                <table class="table">
                    <thead>
                        <tr>
                        <th>#</th>
                        <th>vocabulary</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var indexInPerlesson=0,i=1; i < vocabularyinform.length; i++) { %>
                           <% if(vocabularyinform[i].lessonNumTable==lessonindex){ %> 
                                <% indexInPerlesson++ %>
                                <tr>
                                <th scope="row"><%=indexInPerlesson %></th> 
                                <td>
                                    <%= vocabularyinform[i].vocabularyTable%>
                                    <!-- update/delete vocabulary -->
                                    <% if (loginStatus){ %>
                                        
                                    <a href="/lesson/<%= lessonindex %>/deleteVocabulary/<%= vocabularyinform[i]._id %>" class="btn btn-default pull-right">刪除</a>
                                    <button class = "btn btn-default pull-right " data-toggle="modal" data-target="#editWord<%= i %>">
                                        <span class="glyphicon glyphicon-pencil " aria-hidden ="true"></span>編輯
                                    </button>
                                    <% } %>
                                    <!-- update vocabulary Modal -->
                                    <div class="modal fade" id="editWord<%= i %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                        <form class="form-horizontal" role="form" action="/lesson/<%= lessonindex %>/editVocabulary/<%= vocabularyinform[i]._id %>" method="post">
                                            <div class="modal-dialog" role="document">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <button type = "button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <div class="input-group">
                                                            <input type="text" class="form-control" name="vocabularyUpdate" id="vocabularyUpdate" value=<%= vocabularyinform[i].vocabularyTable%> required="true" autofocus>
                                                            <span class="input-group-btn">
                                                                    <button type = "submit" class="btn btn-default ">儲存變更</button>
                                                            </span>
                                                        </div>                                      
                                                    </div>
                                                </div>
                                            </div>
                                        </form>
                                    </div>
                                </td>                 
                                </tr>
                                <% } %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </section>
    </body>
</html>